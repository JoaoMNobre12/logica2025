#include <stdio.h>

int main() {
    int A, B, PROD;

    // Leitura dos dois inteiros
    scanf("%d", &A);
    scanf("%d", &B);

    // Cálculo do produto
    PROD = A * B;

    // Impressão da saída no formato exigido
    printf("PROD = %d\n", PROD);

    return 0;
}
